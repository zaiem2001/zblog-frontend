import styled from "styled-components";
import InfiniteScroll from "react-infinite-scroller";

export const StyledInfiniteScrollContainer = styled(InfiniteScroll)`
  display: flex;
  flex-direction: column;
`;
